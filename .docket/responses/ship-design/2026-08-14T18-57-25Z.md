# Question Surface — how it ships — responses

Questionnaire: `ship-design`  
Submitted: 2026-08-14T18:57:25Z  
Respondent: Alexander van Rossum  
Answered 17 / 20 · flagged unknown 2 · left blank 1

> **Needs research:** `repo-name`, `project-dir-name`

## Shape of the install

### What is the primary install route?

*`git clone` + `./install.sh`*

<sub>matches recommendation</sub>

### Should the installer symlink the skill into `~/.claude/skills/`, or copy it?

*Symlink*

<sub>matches recommendation</sub>

### Public or private repository?

*Public*

<sub>matches recommendation</sub>

### Repository name

*Don't know — needs research*

**Notes:** Open to suggestions.  That was just literally what i said when i built it "OMG i can't handle this any more i need a question surface" - i know it's not a great name.

### Who creates and pushes the GitHub repo?

*I create it with `gh` and push, then hand you the URL*

**Notes:** [redacted — git identity to use for this repo]

<sub>matches recommendation</sub>

## Where questionnaires and responses live

### Where do questionnaires and responses go?

*In the project being worked on*

<sub>matches recommendation</sub>

### What is that directory called inside a project?

*Don't know — needs research*

**Notes:** somthign unlikely to create a conflict / collision, but also very clearly identifies what it is, and preferably infomred by the name of the repo

### Should responses be committed to the host project's git by default?

*Tracked — the installer writes no ignore rule*

<sub>matches recommendation</sub>

### Should `qsurface` stage the response file itself after a submit?

*No — the tool never touches git*

**Notes:** ... i like "qsurface" 'cause it sounds neat.  I don't think it's a good name, but it's fun to say.  Surface sounds like a MS product (it is lol) and i think qsurface kinda sounds like it too

<sub>matches recommendation</sub>

## The loop an agent drives

### Keep `serve` blocking, or add a non-blocking start/stop pair?

*Blocking only*

<sub>matches recommendation</sub>

### Should waiting time out?

*`--timeout`, with a default*

<sub>matches recommendation</sub>

### What happens when it times out?

*Save the draft, exit non-zero, write nothing*

<sub>matches recommendation</sub>

### What is the default gate, for someone who never configures one?

*Five or more*

**Notes:** .... so this surface REALLY improves my ability to think about how to answer stuff.  so.

i think the gate should be widened:

1) five or more, by default
2) if the questions (any amount) that need answers would _benefit from aditional exposition or detailed answer choices and opened ended interaction_

<sub>matches recommendation</sub>

### How does a user change that number for themselves?

*A config file, plus a one-line pointer the installer adds to your global `CLAUDE.md`*

<sub>matches recommendation</sub>

## Reach and durability

### Expose the tool over MCP as well as the CLI?

*Not now — revisit once the CLI is in use*

**Notes:** Add to roadmap for sure.

<sub>matches recommendation</sub>

### Add a `spec_version` field to the questionnaire format?

*Yes — validated, defaulting to 1 when absent*

<sub>matches recommendation</sub>

### Add GitHub Actions running the test suite?

*Yes — on push and PR, across supported Python versions*

**Notes:** ... you know... i wasn't being serious about that - it's a running joke that terrible vibecoders ask that sort of thing.

That said, were were going to do CI anyway, and i appreciate that the reason is "i asked for no bugs".  Legitimately funny.

<sub>matches recommendation</sub>

### Should the client-side JavaScript get automated checks?

*A committed script that drives headless Chrome*

**Notes:** headless chrome seems like it might allow for gaps or forgotten suites.  That said, you get the same with Jest.  Let's start with headless chrome and if that proves insufficient, we can make a JEst exception and build around that.

Though - headless chrome will require deps too.  Perhaps both or Jest only.

<sub>matches recommendation</sub>

### Anything here worth building into v1?

*`qsurface --version` · `qsurface doctor` · `qsurface show --open` · Follow-up questionnaires that carry the prior response as context · `qsurface archive <id>`*

### Anything I have not asked that would change the design?

*(no answer)*
